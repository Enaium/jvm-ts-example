# jvm-ts-example

A small example project showing how to bundle TypeScript assets with a Kotlin JVM application.

## Overview

This repository demonstrates a Kotlin/JVM application that embeds TypeScript-built JavaScript into its resources and runs it via GraalVM's JavaScript engine.